diff --git a/node_modules/@youversion/platform-react-native-expo-ui/README.md b/node_modules/@youversion/platform-react-native-expo-ui/README.md
index 333d445..c476f4b 100644
--- a/node_modules/@youversion/platform-react-native-expo-ui/README.md
+++ b/node_modules/@youversion/platform-react-native-expo-ui/README.md
@@ -58,3 +58,42 @@ The UI `YouVersionProvider` forwards the same optional version filter lists as c
 This SDK is licensed under [Apache 2.0](./LICENSE).
 
 Licensing information for the Bible versions is available at the [YouVersion Platform](https://platform.youversion.com/) site.
+
+
+## Qingmu reader-settings extension (pinned 1.5.0)
+
+This package copy carries a Qingmu-maintained additive patch. These APIs are not
+provided by upstream YouVersion 1.5.0 or 1.6.0. The original LICENSE is unchanged.
+
+The package root exports `getReaderSettings`, `getDefaultReaderSettings`,
+`setReaderSettings`, `subscribeReaderSettings`, and the `ReaderSettingsSnapshot`
+type. Snapshots contain only fontSize, fontFamily and lineSpacing. The original
+Reader and Settings UI continue using the same store and their original setters.
+
+`setReaderSettings` validates the entire partial object before one SDK store
+update and one SDK persistence write. Font sizes are clamped by the SDK; finite
+unsupported line spacing normalizes to the SDK default. Font families must match
+the built-in Inter/Untitled Serif stacks; the legacy Source Serif stack migrates
+through the existing SDK normalizer. Malformed numbers, unknown keys and invalid
+font strings throw TypeError without changing state. No private persistence key
+is read or written by the facade. Subscribers receive detached snapshots only
+when one of the three values changes; subscription does not emit immediately.
+
+This API does not itself provide account isolation. The host application owns
+per-member SecureStore values and its applying guard during account restoration.
+
+### Maintenance and original-file hash guard
+
+Only apply to package version 1.5.0 with these exact original SHA-256 values.
+Do not treat an upgrade or a changed file as compatible merely because a hunk
+applies. Rebase this tracked patch and rerun sdkReaderSettingsExtension.test.ts
+plus the declaration compile probe before accepting another SDK build.
+
+- `README.md`: `7570dc3e9b7ec54f17d15771c5bba1bf650cb13971e0db350f7ccd78a60901d6`
+- `build/index.js`: `756f0c78b8d6cdbbf4019f0813188d6bf790469cd83d96fed53d35fbf2ba7261`
+- `build/index.d.ts`: `9abafc4fa24f58ef4a2798509ecec9bbcb0bf0e49a662ec9c66dae76f87ae543`
+- `build/stores/reader-settings-store.js`: `55510c92bf4b836e05bfa8dae750cd7f2e903b41b49e7dffeca67619eb5411ec`
+- `build/stores/reader-settings-store.d.ts`: `81f41b70940370dbec1abe0e657bc72b9cc360dc5f75eb609ec8d304c5c1b10d`
+
+Project patch: patches/@youversion+platform-react-native-expo-ui+1.5.0.patch
+Project receipt: docs/receipts/sdk-reader-settings-extension-20260912.json
diff --git a/node_modules/@youversion/platform-react-native-expo-ui/build/dom/bible-reader.js b/node_modules/@youversion/platform-react-native-expo-ui/build/dom/bible-reader.js
index 10cc5be..313843b 100644
--- a/node_modules/@youversion/platform-react-native-expo-ui/build/dom/bible-reader.js
+++ b/node_modules/@youversion/platform-react-native-expo-ui/build/dom/bible-reader.js
@@ -68,7 +68,7 @@ export default function BibleReaderDOM(props) {
     // fontSize/fontFamily use controlled props (not CSS overrides like bg/fg)
     // because the in-WebView toolbar also mutates them — controlled props keep
     // MMKV and the Web SDK's internal state in sync bidirectionally.
-    const providerContent = (_jsxs(_Fragment, { children: [_jsx("style", { href: "yv-bible-reader-host-height", precedence: "medium", children: `html, body, #root { height: 100%; }` }), _jsx("style", { href: "yv-bible-reader-overrides", precedence: "medium", children: `[data-slot="yv-bible-renderer"] {
+    const providerContent = (_jsxs(_Fragment, { children: [_jsx("style", { href: "yv-bible-reader-host-height", precedence: "medium", children: `html, body, #root { height: 100%; }` }), _jsx("style", { href: "qingmu-reader-compact-header", precedence: "medium", children: `[data-yv-sdk] > main { padding-top: 16px !important; gap: 16px !important; } [data-yv-sdk] > main > h1:not(:has(svg)) { display: none !important; }` }), _jsx("style", { href: "qingmu-verse-highlight-padding", precedence: "medium", children: ":is([data-slot=yv-bible-renderer],[data-yv-sdk-bible-reader]) .yv-v { padding-inline: 0 !important; }" }), _jsx("style", { href: "yv-bible-reader-overrides", precedence: "medium", children: `[data-slot="yv-bible-renderer"] {
           ${backgroundColor ? `--yv-reader-bg: ${sanitizeCssValue(backgroundColor)} !important;` : ''}
           ${foregroundColor ? `--yv-reader-fg: ${sanitizeCssValue(foregroundColor)} !important;` : ''}
         }` }), bottomScrollPadding > 0 && (_jsx("style", { href: "yv-bible-reader-scroll-padding", precedence: "medium", children: `[data-yv-sdk] > main {
diff --git a/node_modules/@youversion/platform-react-native-expo-ui/build/index.d.ts b/node_modules/@youversion/platform-react-native-expo-ui/build/index.d.ts
index c48ec7c..d28e766 100644
--- a/node_modules/@youversion/platform-react-native-expo-ui/build/index.d.ts
+++ b/node_modules/@youversion/platform-react-native-expo-ui/build/index.d.ts
@@ -4,4 +4,8 @@ export { useSignOutGuard } from './native';
 export { useTokens } from './hooks';
 export { getTokens } from './theme';
 export type { Tokens } from './theme';
+/** Qingmu extension; these APIs are not supplied by upstream SDK 1.5.0/1.6.0. */
+export { mergePlayingVerseHighlight, PLAYING_VERSE_HIGHLIGHT_COLOR } from './native/bible-reader';
+export { getReaderSettings, getDefaultReaderSettings, setReaderSettings, subscribeReaderSettings } from './stores/reader-settings-store';
+export type { ReaderSettingsSnapshot } from './stores/reader-settings-store';
 //# sourceMappingURL=index.d.ts.map
\ No newline at end of file
diff --git a/node_modules/@youversion/platform-react-native-expo-ui/build/index.js b/node_modules/@youversion/platform-react-native-expo-ui/build/index.js
index b47510f..4b2315a 100644
--- a/node_modules/@youversion/platform-react-native-expo-ui/build/index.js
+++ b/node_modules/@youversion/platform-react-native-expo-ui/build/index.js
@@ -2,6 +2,10 @@ import { ensureDomImpls } from './native/register-dom-impls';
 ensureDomImpls();
 export { BibleCard, BibleChapterPickerSheet, BibleReader, BibleReaderSettingsSheet, BibleTextView, BibleVersionPickerSheet, VerseOfTheDay, YouVersionAuthButton, YouVersionProvider, } from './native';
 export { useSignOutGuard } from './native';
+// Qingmu extension: transient reading highlight helper (see README).
+export { mergePlayingVerseHighlight, PLAYING_VERSE_HIGHLIGHT_COLOR } from './native/bible-reader';
 export { useTokens } from './hooks';
 export { getTokens } from './theme';
+// Qingmu extension; maintained by the host project for pinned SDK 1.5.0.
+export { getReaderSettings, getDefaultReaderSettings, setReaderSettings, subscribeReaderSettings } from './stores/reader-settings-store';
 //# sourceMappingURL=index.js.map
\ No newline at end of file
diff --git a/node_modules/@youversion/platform-react-native-expo-ui/build/lib/web-yv-provider.js b/node_modules/@youversion/platform-react-native-expo-ui/build/lib/web-yv-provider.js
index 4d1e6a4..a20ded1 100644
--- a/node_modules/@youversion/platform-react-native-expo-ui/build/lib/web-yv-provider.js
+++ b/node_modules/@youversion/platform-react-native-expo-ui/build/lib/web-yv-provider.js
@@ -1,15 +1,16 @@
 // Import first: installs the Android `localStorage` shim (see dom-local-storage)
 // before the Web SDK module below evaluates and any provider renders.
 import { ensureDomLocalStorage } from './dom-local-storage';
-import { YouVersionProvider as BaseYouVersionProvider } from '@youversion/platform-react-ui';
+import { YouVersionProvider as BaseYouVersionProvider, YouVersionPlatformConfiguration } from '@youversion/platform-react-ui';
 import { createElement } from 'react';
 import { ensureDomContentCache } from './dom-content-cache';
 import { mergeSdkHeaders } from './sdk-version';
 ensureDomLocalStorage();
 ensureDomContentCache();
-export function YouVersionProvider({ additionalHeaders, ...rest }) {
+export function YouVersionProvider({ additionalHeaders, apiHost, ...rest }) {
     return createElement(BaseYouVersionProvider, {
         ...rest,
+        apiHost: apiHost ?? YouVersionPlatformConfiguration.apiHost,
         additionalHeaders: mergeSdkHeaders(additionalHeaders),
     });
 }
diff --git a/node_modules/@youversion/platform-react-native-expo-ui/build/native/bible-reader.d.ts b/node_modules/@youversion/platform-react-native-expo-ui/build/native/bible-reader.d.ts
index 103616e..7baf326 100644
--- a/node_modules/@youversion/platform-react-native-expo-ui/build/native/bible-reader.d.ts
+++ b/node_modules/@youversion/platform-react-native-expo-ui/build/native/bible-reader.d.ts
@@ -1,3 +1,4 @@
+import type { Highlight } from '@youversion/platform-react-native-expo-core';
 import type { BibleReaderShareData, BibleVersionPickerPressData, FootnoteData } from '@youversion/platform-react-ui';
 import type { Ref } from 'react';
 import { type ReactNode } from 'react';
@@ -51,11 +52,19 @@ export type BibleReaderProps = Omit<DomBibleReaderProps, 'appKey' | 'highlights'
      * screen; render an offline or pending hint rather than an error toast.
      */
     onHighlightError?: (error: HighlightWriteError) => void;
+    /**
+     * Qingmu extension. Verse number currently being narrated in the displayed chapter, painted as a
+     * transient light-grey highlight through the controlled highlights channel. `null` paints nothing.
+     * Never scrolls the reader.
+     */
+    playingVerse?: number | null;
     /**
      * Imperative handle — see {@link BibleReaderHandle}. React 19 passes `ref`
      * as an ordinary prop, so there is no `forwardRef` here.
      */
     ref?: Ref<BibleReaderHandle>;
 };
-export declare function BibleReader({ theme, book: controlledBook, defaultBook, onBookChange, chapter: controlledChapter, defaultChapter, onChapterChange, versionId: controlledVersionId, defaultVersionId, onVersionChange, showToolbar, onChapterPickerPress: consumerOnChapterPickerPress, onVersionPickerPress: consumerOnVersionPickerPress, onFootnotePress: consumerOnFootnotePress, onVerseSelect, clearSelectionSignal, onCopy: consumerOnCopy, onShare: consumerOnShare, onHighlightError, backgroundColor, foregroundColor, dom, ref, }: BibleReaderProps): ReactNode;
+export declare const PLAYING_VERSE_HIGHLIGHT_COLOR: string;
+export declare function mergePlayingVerseHighlight(highlights: Highlight[], scope: { versionId: number | undefined; book: string | undefined; chapter: string | undefined; playingVerse: number | null | undefined }): Highlight[];
+export declare function BibleReader({ theme, book: controlledBook, defaultBook, onBookChange, chapter: controlledChapter, defaultChapter, onChapterChange, versionId: controlledVersionId, defaultVersionId, onVersionChange, showToolbar, onChapterPickerPress: consumerOnChapterPickerPress, onVersionPickerPress: consumerOnVersionPickerPress, onFootnotePress: consumerOnFootnotePress, onVerseSelect, playingVerse, clearSelectionSignal, onCopy: consumerOnCopy, onShare: consumerOnShare, onHighlightError, backgroundColor, foregroundColor, dom, ref, }: BibleReaderProps): ReactNode;
 //# sourceMappingURL=bible-reader.d.ts.map
\ No newline at end of file
diff --git a/node_modules/@youversion/platform-react-native-expo-ui/build/native/bible-reader.js b/node_modules/@youversion/platform-react-native-expo-ui/build/native/bible-reader.js
index e6974a0..dceb530 100644
--- a/node_modules/@youversion/platform-react-native-expo-ui/build/native/bible-reader.js
+++ b/node_modules/@youversion/platform-react-native-expo-ui/build/native/bible-reader.js
@@ -54,7 +54,22 @@ const NO_PROMPT = { kind: 'none' };
 function sameScope(a, b) {
     return a.versionId === b.versionId && a.book === b.book && a.chapter === b.chapter;
 }
+/**
+ * Qingmu extension (not upstream 1.5.0/1.6.0): paint the verse the narrator is reading as a
+ * transient highlight. Reuses the controlled highlights channel so the DOM reader paints it exactly
+ * like a user highlight and never scrolls. The entry is derived, never written to the user's
+ * highlights; a user highlight on the same verse is shadowed only while that verse is playing.
+ */
+export const PLAYING_VERSE_HIGHLIGHT_COLOR = 'd5ddd8';
+export function mergePlayingVerseHighlight(highlights, { versionId, book, chapter, playingVerse }) {
+    if (!Number.isInteger(playingVerse) || playingVerse < 1 || !Number.isInteger(versionId) || !book || !chapter) return highlights;
+    const passageId = `${book}.${chapter}.${playingVerse}`;
+    const rest = highlights.filter((entry) => !(entry.version_id === versionId && entry.passage_id === passageId));
+    return [...rest, { version_id: versionId, passage_id: passageId, color: PLAYING_VERSE_HIGHLIGHT_COLOR }];
+}
 export function BibleReader({ theme, book: controlledBook, defaultBook = DEFAULT_BOOK, onBookChange, chapter: controlledChapter, defaultChapter = DEFAULT_CHAPTER, onChapterChange, versionId: controlledVersionId, defaultVersionId = DEFAULT_BIBLE_VERSION_ID, onVersionChange, showToolbar = true, onChapterPickerPress: consumerOnChapterPickerPress, onVersionPickerPress: consumerOnVersionPickerPress, onFootnotePress: consumerOnFootnotePress, onVerseSelect, 
+// Qingmu extension: transient reading highlight (see README).
+playingVerse = null, 
 // Defaulted to `0` so a number always crosses the bridge from first mount.
 // The Web SDK treats the value it sees at mount as a baseline and clears the
 // selection on every change after that. Leaving this `undefined` means a
@@ -379,7 +394,7 @@ clearSelectionSignal = 0, onCopy: consumerOnCopy, onShare: consumerOnShare, onHi
         ...dom,
         style: StyleSheet.flatten([dom?.style, { flex: 1 }]),
     }), [dom]);
-    return (_jsxs(_Fragment, { children: [_jsx(View, { style: { flex: 1 }, children: _jsx(BibleReaderDOM, { ...authProps, appKey: context.appKey, apiHost: context.apiHost, installationId: context.installationId, fetchBibleContent: context.fetchBibleContent, permittedVersionIds: context.permittedVersionIds, excludedVersionIds: context.excludedVersionIds, permittedLanguageTags: context.permittedLanguageTags, locale: lng, highlights: highlights, verseActions: VERSE_ACTIONS, onVerseSelect: handleVerseSelect, clearSelectionSignal: clearSelectionSignal + internalClearCount, onSignInPress: signIn, onSignOutPress: guardedSignOut, userInfo: userInfo, theme: resolvedTheme, book: book, chapter: chapter, versionId: versionId, fontSize: fontSize, fontFamily: encodeFontFamilyForDom(fontFamily), lineSpacing: lineSpacing, onFontSizeChange: setFontSize, onFontFamilyChange: setFontFamily, onLineSpacingChange: setLineSpacing, onOpenBibleThemeSettings: Platform.OS !== 'web' ? handleOpenBibleThemeSettings : undefined, onBookChange: handleBookChange, onChapterChange: handleChapterChange, onVersionChange: handleVersionChange, showToolbar: showToolbar, onChapterPickerPress: handleChapterPickerPress, onVersionPickerPress: handleVersionPickerPress, onFootnotePress: onFootnotePress, onExternalLinkPress: Platform.OS !== 'web' ? onExternalLinkPress : undefined, backgroundColor: backgroundColor, foregroundColor: foregroundColor, bottomScrollPadding: bottomScrollPadding, dom: readerDom }) }), Platform.OS !== 'web' && (_jsx(BibleReaderSettingsSheet, { isSettingsSheetOpen: isSettingsSheetOpen, onClose: () => setIsSettingsSheetOpen(false) })), Platform.OS !== 'web' && (_jsx(BibleVerseActionSheet
+    return (_jsxs(_Fragment, { children: [_jsx(View, { style: { flex: 1 }, children: _jsx(BibleReaderDOM, { ...authProps, appKey: context.appKey, apiHost: context.apiHost, installationId: context.installationId, fetchBibleContent: context.fetchBibleContent, permittedVersionIds: context.permittedVersionIds, excludedVersionIds: context.excludedVersionIds, permittedLanguageTags: context.permittedLanguageTags, locale: lng, highlights: mergePlayingVerseHighlight(highlights, { versionId, book, chapter, playingVerse }), verseActions: VERSE_ACTIONS, onVerseSelect: handleVerseSelect, clearSelectionSignal: clearSelectionSignal + internalClearCount, onSignInPress: signIn, onSignOutPress: guardedSignOut, userInfo: userInfo, theme: resolvedTheme, book: book, chapter: chapter, versionId: versionId, fontSize: fontSize, fontFamily: encodeFontFamilyForDom(fontFamily), lineSpacing: lineSpacing, onFontSizeChange: setFontSize, onFontFamilyChange: setFontFamily, onLineSpacingChange: setLineSpacing, onOpenBibleThemeSettings: Platform.OS !== 'web' ? handleOpenBibleThemeSettings : undefined, onBookChange: handleBookChange, onChapterChange: handleChapterChange, onVersionChange: handleVersionChange, showToolbar: showToolbar, onChapterPickerPress: handleChapterPickerPress, onVersionPickerPress: handleVersionPickerPress, onFootnotePress: onFootnotePress, onExternalLinkPress: Platform.OS !== 'web' ? onExternalLinkPress : undefined, backgroundColor: backgroundColor, foregroundColor: foregroundColor, bottomScrollPadding: bottomScrollPadding, dom: readerDom }) }), Platform.OS !== 'web' && (_jsx(BibleReaderSettingsSheet, { isSettingsSheetOpen: isSettingsSheetOpen, onClose: () => setIsSettingsSheetOpen(false) })), Platform.OS !== 'web' && (_jsx(BibleVerseActionSheet
             // Yielded, not displaced. A prompt that takes over the sheet host
             // would fire this sheet's `onClose`, which clears the selection the
             // prompt's own answer still needs.
diff --git a/node_modules/@youversion/platform-react-native-expo-ui/build/stores/reader-settings-store.d.ts b/node_modules/@youversion/platform-react-native-expo-ui/build/stores/reader-settings-store.d.ts
index bcd2248..c2292b8 100644
--- a/node_modules/@youversion/platform-react-native-expo-ui/build/stores/reader-settings-store.d.ts
+++ b/node_modules/@youversion/platform-react-native-expo-ui/build/stores/reader-settings-store.d.ts
@@ -24,5 +24,23 @@ export declare const useReaderSettingsStore: import("zustand").UseBoundStore<Omi
         getOptions: () => Partial<import("zustand/middleware").PersistOptions<ReaderSettingsState, unknown, unknown>>;
     };
 }>;
-export {};
+/** Qingmu extension for the pinned SDK 1.5.0; not an upstream YouVersion API. */
+export type ReaderSettingsSnapshot = {
+    fontSize: number;
+    fontFamily: string;
+    lineSpacing: number;
+};
+/** Returns a detached snapshot of the same store used by the official UI. */
+export declare function getReaderSettings(): ReaderSettingsSnapshot;
+/** Returns a fresh snapshot of the SDK defaults, independent of current state. */
+export declare function getDefaultReaderSettings(): ReaderSettingsSnapshot;
+/**
+ * Validates all fields before one atomic store/persistence update. Numbers must
+ * be finite; SDK font-size clamps and line-spacing normalization apply. Fonts
+ * must be the built-in canonical SDK stacks (legacy Source Serif is migrated).
+ * Unknown keys or malformed values throw TypeError without changing state.
+ */
+export declare function setReaderSettings(partial: Partial<ReaderSettingsSnapshot>): void;
+/** Emits detached snapshots only on value changes; does not emit immediately. */
+export declare function subscribeReaderSettings(listener: (snapshot: ReaderSettingsSnapshot) => void): () => void;
 //# sourceMappingURL=reader-settings-store.d.ts.map
\ No newline at end of file
diff --git a/node_modules/@youversion/platform-react-native-expo-ui/build/stores/reader-settings-store.js b/node_modules/@youversion/platform-react-native-expo-ui/build/stores/reader-settings-store.js
index 2e79053..0783798 100644
--- a/node_modules/@youversion/platform-react-native-expo-ui/build/stores/reader-settings-store.js
+++ b/node_modules/@youversion/platform-react-native-expo-ui/build/stores/reader-settings-store.js
@@ -4,7 +4,7 @@ import { create } from 'zustand';
 import { createJSONStorage, persist } from 'zustand/middleware';
 import { mmkvStorage } from '@youversion/platform-react-native-expo-core';
 import { READER_SETTINGS_PERSIST_KEY } from '../lib/constants';
-import { SOURCE_SERIF_FONT, UNTITLED_SERIF_FONT } from '../lib/reader-fonts';
+import { INTER_FONT, SOURCE_SERIF_FONT, UNTITLED_SERIF_FONT } from '../lib/reader-fonts';
 import { READER_LINE_SPACING } from './types/reader-line-spacing';
 /** MMKV-backed storage for zustand `persist` (sync; hydrates at store creation). */
 const mmkvStateStorage = {
@@ -75,4 +75,47 @@ export const useReaderSettingsStore = create()(persist((set) => ({
         };
     },
 }));
+// Qingmu extension for SDK 1.5.0; not an upstream YouVersion API.
+// Reuse this exact store so the official Reader and Settings UI stay consistent.
+const snapshotOf = (state) => ({ fontSize: state.fontSize, fontFamily: state.fontFamily, lineSpacing: state.lineSpacing });
+const sameSettings = (left, right) => left.fontSize === right.fontSize && left.fontFamily === right.fontFamily && left.lineSpacing === right.lineSpacing;
+const supportedFonts = new Set([INTER_FONT, UNTITLED_SERIF_FONT, SOURCE_SERIF_FONT]);
+export function getReaderSettings() {
+    return snapshotOf(useReaderSettingsStore.getState());
+}
+export function getDefaultReaderSettings() {
+    return { fontSize: BIBLE_READER_FONT.DEFAULT, fontFamily: UNTITLED_SERIF_FONT, lineSpacing: READER_LINE_SPACING.DEFAULT };
+}
+export function setReaderSettings(partial) {
+    if (partial === null || typeof partial !== 'object' || Array.isArray(partial)) {
+        throw new TypeError('Reader settings must be an object');
+    }
+    const next = getReaderSettings();
+    for (const key of Object.keys(partial)) {
+        const value = partial[key];
+        if (key === 'fontSize' || key === 'lineSpacing') {
+            if (typeof value !== 'number' || !Number.isFinite(value)) {
+                throw new TypeError('Reader setting must be a finite number');
+            }
+            next[key] = key === 'fontSize' ? clampBibleReaderFontSize(value) : normalizeLineSpacing(value);
+        } else if (key === 'fontFamily') {
+            if (typeof value !== 'string' || value.length === 0 || value.length > 256 || !supportedFonts.has(value)) {
+                throw new TypeError('Reader font must be a supported SDK font family');
+            }
+            next.fontFamily = normalizeFontFamily(value);
+        } else {
+            throw new TypeError('Unknown reader setting');
+        }
+    }
+    if (sameSettings(next, getReaderSettings())) return;
+    // Zustand persist wraps this existing setState: one state notification and
+    // one SDK-owned persistence write, with the original action methods retained.
+    useReaderSettingsStore.setState(next);
+}
+export function subscribeReaderSettings(listener) {
+    if (typeof listener !== 'function') throw new TypeError('Reader settings listener must be a function');
+    return useReaderSettingsStore.subscribe((state, previous) => {
+        if (!sameSettings(state, previous)) listener(snapshotOf(state));
+    });
+}
 //# sourceMappingURL=reader-settings-store.js.map
\ No newline at end of file
