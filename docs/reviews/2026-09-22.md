# 青牧 App Code Review 摘要

本次檢查原始差異 `e4e7878...4300440`，涵蓋資料正確性、安全、效能、既有 UI 行為及建置/發布流程。本文轉寫已核實的 review 與收據，不另行擴大檢查範圍。

使用者要求：**不破壞、不複雜化既有學生功能，不新增產品功能；保留四 tabs 與讀經/音訊語意。** 離線下載不在本輪範圍。

| 批次 | 狀態 |
|---|---|
| P1 | [PR #1](https://github.com/oxydavid-maxx/spllt-bible/pull/1) 已合併，`d6c813d` 已部署並核實。 |
| P2 | [PR #2](https://github.com/oxydavid-maxx/spllt-bible/pull/2) 仍為 draft；主要修正候選為 `b80500b`、APK 為 0.5.8/code29。後續 debug 修正為 `b048804`，scanner/Camera patch 尚屬後續候選；不能沿用舊 APK 的驗收結論。 |

## P1：已部署

| ID | 發現與影響 | 修正與證據範圍 |
|---|---|---|
| F01 | 學生提案進入具主機工具與自訂設定的 Claude process，存在超出文字估分用途的風險。 | [CLI 邊界](../../server/claudeCli.ts) 限制 tools/MCP/自訂設定與 session persistence。實際正常估分、合成注入與部署/rollback 檢查已完成。 |
| F02 | 公告 publisher 未先同步 main；push 失敗後同內容重跑可能誤報無變更，公告留在本機。 | [Publisher](../../tools/announce/publisher.ts) 同步 main、恢復公告限定 commit、只 stage 指定檔案並回讀 remote；真實暫存 Git 情境與現役無變更路徑已核實。 |

## P2：修正摘要

以下為已核實的 source/重現發現，信心高。「已修碼」不代表所有實機控制已通過。

| ID/範圍 | 原問題與修正 | 目前狀態/來源 |
|---|---|---|
| F03–F05 資料與隱私 | 非 OPEN 提案占用三票；截止/關閉後仍可能改動；逐章人數繞過小群體隱私門檻。修正額度計算、輪次/期限檢查及相同人數門檻，保留歷史紀錄。 | 已修碼、focused 驗證；[提案](../../server/rewardNominations.ts)、[共同進度](../../server/communityProgress.ts)。 |
| F06–F07 快取與生命週期 | Cache 被慢網路阻擋、重複/離頁 GET，以及舊回覆/操作鎖競態。改為先顯示舊資料、GET 期限/取消/合併、focus 管理及 await 後 owner 檢查；保留 mutation 的 operation ID、恢復方式與失敗草稿。 | 已修碼、focused 驗證；[ProgressScreen](../../app/(tabs)/progress.tsx)、[公告 client](../../src/services/announcementClient.ts)、[API client](../../src/services/gamificationApiClient.ts)。 |
| F08–F09 公告匯入與保全 | XLSX 空欄使經文移到講員欄；來源故障清空公告；舊 fallback 可覆寫 remote 修正。改依 cell reference 定位，必要來源失敗不發布，歷史資料採有效 fallback；reconcile 後快照不同就停止寫入。 | 已修碼、XLSX/來源與真實 Git fixture 驗證；[Office parser](../../tools/announce/office.ts)、[Builder](../../tools/announce/build.ts)、[Publisher](../../tools/announce/publisher.ts)。 |
| F10 Release 建置 | 非空測試值、大小寫或空白值可混入 release；檢查晚於 native 修改，dotenv 可補入未驗證值，receipt 漏記 Minify。改為正式入口先驗證、release 禁 dotenv，保留 private process binding，記錄實際參數與 Minify。 | 已修碼，Windows PowerShell 5.1/UTF-8 BOM 與入口情境已驗證；[Build entry](../../scripts/build-android.ps1)、[Env guard](../../scripts/release-environment.ps1)。 |
| F11 背景工作關閉 | 估分 worker 未等待目前工作結束，可能在 DB 關閉後繼續操作。停止新 tick、等待工作，提供冪等可等待的 stop。 | 已修碼、受控 shutdown 驗證；[Worker](../../server/nominationAssist.ts)、[HTTP lifecycle](../../server/http.ts)。Windows 原生 signal 不以 Node 測試宣稱通過。 |
| F12 Reader 工具 | 更多工具 sheet 在實機截斷倍速列與版本資訊。調整有界捲動與高度鏈，保留內容及讀經/音訊行為。 | **code29 已觀察修復**：完整列、版本資訊及官方連結/返回；其他 reader/audio 控制仍待完整驗收。[Layout](../../src/ui/FullscreenReaderLayout.tsx)。 |
| F14–F15 Debug/Camera | Debug deny-all policy 阻擋本機端點；camera preview 候選改用 `COMPATIBLE`，且只將 expo-camera 指向 source build。 | Debug policy 生成已驗證且 release 位元組不變；camera hunk/版本及 autolinking routing 已核對，**preview/scan 未列 PASS**。[Network plugin](../../plugins/withQingmuNetworkSecurity.js)、[Camera patch](../../patches/expo-camera+56.0.8.patch)、[設定](../../package.json)。 |

既有 `postinstall: patch-package --error-on-fail` 沒有 package 篩選，新增 Camera patch 隨 PR 提交後可納入原套用流程；版本對應 lock 的 56.0.8。檔案存在與 routing 正確不等於 patch 已進入新的 native artifact，仍由 native owner 完成重建/安裝證據。

## Scanner 追加：兩項修正中

| ID/級別 | 具體問題 | 狀態 |
|---|---|---|
| F16/P2 | 有效 QR 的 claim pending 時關閉 scanner，晚成功回覆仍可能導向好友頁；[FriendQrPanel](../../src/ui/gamification/FriendQrPanel.tsx) 與 [路由](../../app/(tabs)/progress.tsx) 未綁定操作開始時的 sheet/view。 | **修正中**：UI owner 綁定原 sheet/view/session，保留合法 server 結果但忽略過期 UI 導向；覆蓋關閉、切頁及前景變化。 |
| F17/P3 | claim catch 把自己的 QR、登入失效與網路故障統一誤報為「無效或已過期」，API 已有分類訊息。 | **修正中**：重用 [GamificationApiError.userMessage](../../src/services/gamificationApiClient.ts)，未知錯誤給一般重試訊息，沿用現有操作。 |

這兩項是高信心 source call-chain 發現，未宣稱完成 native 重現或修復。限縮 scanner review 未找到其他有足夠證據的 patch 結構/設定問題。

## 尚未完成與發布界線

- **Native 驗收尚未完成。** 必須完成原有控制矩陣與一天的使用流程，包含讀經/音訊、日記、積分、登入、相機、通知、離線恢復及各角色/條件狀態；preview/scan 仍待核實。
- F13：候選的 [更新 manifest](../../announcements/app-version.json) 尚未切換至這次已驗證 APK 的具體資產。完成驗收後才發布 release、更新連結並回讀；不宣稱原 `/releases/latest` 導向問題已結案。
- 各 focused 收據有重疊，**不加總測試數**；API、Node、Git fixture 或 debug 建置不能代替 Android 全流程驗收。
- 完成帳號/設定還原；帳務透過正常反向操作還原淨額並保留 audit。之後才合併/發布，安排家庭小範圍試用，再擴大使用。

## UIUX 建議

- 保留四 tabs 與既有讀經/音訊語意，以字級、間距與清楚層級改善閱讀，不加入新的學生設定流程。
- 已授權相機直接開啟；首次授權返回後維持原操作脈絡，使用者關閉後不被晚回覆拉走。
- 三票規則、pending/具體錯誤及 cache 舊資料標示放在操作附近，沿用原本的重試入口。
- 維持 sheet 關閉入口、完整捲動與大字/鍵盤可達性。Reader 視覺 prototype **仍待核准**，不視為已交付改版。
