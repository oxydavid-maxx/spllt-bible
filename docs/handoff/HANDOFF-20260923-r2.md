# 青牧 App 接手單 r2(2026-09-23 06:00 UTC)

寫給下一位 AI。使用者是**光佑**，只用這個稱呼。回覆用繁體中文，技術名詞保留原文。
前一版接手單在本機 `.handoff/CLAUDE-HANDOFF-20260923.md`(git-excluded)。本檔是它之後的最新狀態。
本機各工單報告在 `C:\dev\apps\qingmu-bible\.handoff\work-20260923\`(W1/W2/W3/W4/W7/W10/BUILD 報告)。

## 光佑本輪已拍板

- 「一起走過」(書名列表 + 點燈區 currentBook):**超過 5 人(≥6)才顯示**;累計天次仍 10 人。已實作於 `server/communityProgress.ts`
- 讀經器採用底部固定播放列(Codex 9/22 提案 `.handoff/review-20260922/reader-prototype/`)。已實作
- 後端切換到 git 釘選 worktree 並退役 `qingmu-youth`:已授權、已完成

## 分支與 commit

| 分支 | 內容 | 狀態 |
|---|---|---|
| `release/0.5.9` @ `6f37bfc` | PR #2(8e2fb4b) + 高亮修補 + 底部播放列 + 測試修正 + 版本 0.5.9/code30 + ops 後端從 git 跑(7fe612f merge) + 建置入口防 stale node_modules(6f37bfc) | 已推。**尚未合進 main** |
| `codex/qingmu-p2-repairs` @ `8e2fb4b` | PR #2,GitHub MERGEABLE | 已被 release/0.5.9 包含 |
| `ops/ai-boundary-git-blob-pin` @ `9bcdb7d`(已推) | AI pin 改用 git blob 雜湊 + 啟動時 HEAD 必須等於 worktree 外的 pin 記錄 `<PILOT>\pinned-commit.json`(`{"commit":"<40hex>"}`),49/49 測試 | 待合入 release。**合入後、下次啟動前必須先建立 pin 記錄**,否則 PIN_RECORD_MISSING 拒絕啟動 |

## 已完成且我(Opus)獨立驗證過

- server/tools/domain 測試 409/0 紅;typecheck 乾淨
- 後端:`C:\dev\machine\worktrees\qingmu-bible\r-7fe612f` 以 `ops/start_backend.py` 啟動,health ok,`ai_status=ENABLED_VERIFIED`;Startup 捷徑已改指此處(備份 `%TEMP%\Qingmu Backend.lnk.bak-20260923`)
- `qingmu-youth` 搬到 `C:\dev\machine\quarantine\qingmu-youth-retired-20260923`(收據同目錄,可還原至 2026-10-23)
- DB 切換前備份 `…\QingmuYouthPilot\pilot.sqlite.backup-20260923-cutover`;AI pin 設定備份 `ai-runtime-config.json.bak-20260923-crlf-pin`
- 正式 DB 唯讀實測:1 位成員 → books=[]、currentBook=null、personDays=null
- `~/.claude` dev_layout 新規則 `dev-layout-apps-children`(apps/ 子目錄須為獨立 git、不得重複 app)已上線;它另抓到 5 筆非青牧問題(qingmu-audio-poc、tat-simulator 無 git;兩組共用 remote),待光佑決定
- 手機(0.5.9 第一版 APK)實測:上排一行不換行 ✅、底部播放列 ✅、更多工具全在 ✅、提名看板「選 3 個,還有 3 票」✅(小瑕疵:半形逗號)

## 未完成(依序)

1. ✅ **APK r2 已建好並經 Opus 驗證**:`.handoff/work-20260923/qingmu-youth-0.5.9-code30-r2.apk`,SHA-256 `d54fd36529e5f50585db2ee20d27380f9b9e07d18ba5e1f168b752f8ff83e313`,bundle 內 `trimVerseWrapEdges`=1,由 release/0.5.9@6f37bfc 建出。r1(934e94c6…)作廢勿用
2. **正式 worktree 被動過**:build worker 在 `r-7fe612f` 裡直接 commit 了 6f37bfc(reflog 可見)。後端仍跑 7fe612f 的程式。✅ 已由 Opus 切回 `7fe612f`(乾淨,後端健康)。根治已完成於 `ops/ai-boundary-git-blob-pin`@9bcdb7d:合入 release 後,依 ops/README.md 建新 worktree `r-<sha>`、寫 pin 記錄、重啟、Startup 捷徑改指新目錄;6f37bfc 是事故產物,**不要**把它寫進 pin 記錄。重新釘選時並把私人設定 `ai-runtime-config.json` 的 `boundarySha256` 改成 blob 值(7fe612f 為 `f824c3a47538c0ea82522a1953898b808ae35f6831285b049176a187806aebd0`;新 commit 需重算)
3. **實機驗高亮**(需光佑手機空著,先確認前景是青牧;只用 `adb install -r`):裝 r2 → 讀經器 → 詩98 播放 → 截圖確認第 1 節播放時第 2 節左側**無灰色細條**、經文前無空白色塊。工具:`.handoff/review-20260922/phone.py`(設 `PYTHONIOENCODING=utf-8`)
4. **合併**:release/0.5.9 → main(含關閉 PR #2)
5. **發 GitHub pre-release** + 資產 `qingmu-youth-<SHORTSHA大寫>-<主題>.apk`;再把 `announcements/app-version.json` 的更新網址改指**該資產的直接網址**(不是 `/releases/latest`,它對 pre-release 會 302 到列表)。**一定在第 3 步通過之後**
6. 發布順序:光佑 → 家人 → 學生
7. GitHub About/topics 補積分、獎品、公告(B6)
8. 還原手機設定清單 `.handoff/review-20260922/restore-checklist.md`(譯本 40→46)
9. 光佑待答:D2(「想要」要不要明講可選多個)、D3(要不要開第二個測試帳號)— 一次問一題
10. 舊後端 pid 32992 在 2026-09-23 約 05:02 UTC 自行消失,原因未查明(已排除重開機、睡眠、崩潰紀錄、已知看門狗)

## 環境陷阱(本輪新增)

- **auto mode 會把「子代理被擋 → 主 session 自己做」判為繞過,之後連唯讀指令都擋**。碰正式環境的步驟請光佑切到非 auto 模式
- 派工給 worker 時,務必指定它**自己的新 worktree**,不要讓它挑現有資料夾(本輪它挑中正式後端的 worktree)
- 建置一定走 `scripts/build-android.ps1`(6f37bfc 起會自動對齊 node_modules);不要手動只 checkout 就建
- Codex 額度到 2026-09-29 15:11 前用盡(9/22 rollout 紀錄)
