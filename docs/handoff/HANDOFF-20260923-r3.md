# 青牧 App 接手單 r3（2026-09-23）

接續 [r2](HANDOFF-20260923-r2.md)。產品方向不變：給國高中生使用，保留四個分頁和原有讀經/朗讀操作；先由光佑驗，再給家人，最後才給學生。

## 已核實

| 項目 | 結果 |
|---|---|
| 0.5.9 r2 APK | `0.5.9/code30`，SHA-256 `d54fd36529e5f50585db2ee20d27380f9b9e07d18ba5e1f168b752f8ff83e313`。簽章 SHA-256 `0721aa16599fe2d17b47f951f68e86137fad9c36b6f9db3115f4eb4124038d58` 與前版相同；包內有 `trimVerseWrapEdges`。建置來源是 `6f37bfc`，官方建置收據顯示成功且非 fixture。 |
| 手機安裝 | 已用 `adb install -r` 安裝 r2，裝置回報 0.5.9/code30；保留原資料。安裝時手機前景是 ChatGPT，未送畫面點擊。 |
| 高亮回歸 | 真實《詩篇》98/《提摩太後書》3 HTML 的四項測試通過；`c5896d4` 已 cherry-pick 成 `07cbcaa`。這是程式測試，實機高亮截圖仍待做。 |
| 後端 pin | `ops/ai-boundary-git-blob-pin` 已整合，49 項 ops 測試通過；新 worktree 在合併 commit `79ecbe8`，獨立測試埠啟動/健康檢查通過。正式 8788 已切換至該 commit，外部 commit pin、AI 的 committed blob SHA、啟動收據、port owner、公開健康檢查及開機捷徑均已回讀一致。資料庫與設定的切換前備份保存在 private authority；本地收據在 git 忽略的 `.handoff/work-20260923/`。 |
| GitHub | `release/0.5.9` 已推；About 原本就已更新，本輪只補 `announcements`、`gamification`、`rewards` topics。 |

## 尚未完成的門檻

1. **實機高亮。** 手機目前由光佑使用；等青牧在前景時，驗《詩篇》98 朗讀中的第 1/2 節邊界沒有空白灰條，保存截圖。也確認底部播放列、返回與正常經文可用。不能用包內字串或單元測試代替。
2. **設定還原。** 舊測試把讀經譯本從 46 改成 40；依本地 `restore-checklist.md` 回到 46 並重開確認。不要覆蓋光佑其後自行改的值。
3. **主線發布。** 上述實機驗證通過後，再把 release branch 合進 main、發布附 APK 的 GitHub pre-release，最後把 `announcements/app-version.json` 更新網址改成那個 APK 資產的直接網址並回讀。現在的 `/releases/latest` 對 pre-release 仍不適用。PR #2 仍是 draft；要讓 review 指向 0.5.9 最終候選，再結案。
4. **正式後端觀察。** 新後端目前健康，仍要留意 r2 接手單記載的舊程序自行停止事件；原因尚未查明。若新程序再停，保留啟動/系統事件證據後查根因。

主 checkout 的四個未提交檔案是既存工作；本輪沒有 stage、重設或覆寫。合併 main 時應在隔離 worktree 操作，保留主 checkout 原狀。r1 APK `934e94c6…` 缺高亮 patch，不能用它作本次驗收或發布。
